var xml2rec=require('./xml2rec');

function f()
{
  try {
    xml2rec('hundred.xml', 'assetInflowReport', {
      heading: true,
      format:'csv',
      outputAttributes: true,
      trimWhiteSpaceTextNodes: true,
      trimLeadingSpaces: true,
      trimTrailingSpaces: true
    }, (err, data)=>{
      console.log(data);
    });
  } catch (e) {
    console.log(e);
  } finally {

  }
}

f();
